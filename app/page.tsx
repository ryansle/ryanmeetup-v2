// Components
import { Layout } from '@/components/navigation';
import { Landing } from '@/components/home';

const HomePage = () => {
  return (
    <Layout>
      <Landing />
    </Layout>
  );
};

export default HomePage;